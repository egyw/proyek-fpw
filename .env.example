# MongoDB Connection
# Pilih salah satu: Local MongoDB atau MongoDB Atlas

# Option 1: Local MongoDB (untuk development lokal)
# MONGODB_URI=mongodb://localhost:27017/proyekFPW

# Option 2: MongoDB Atlas (untuk production atau cloud development)
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/proyekFPW?retryWrites=true&w=majority

# NextAuth Configuration
# IMPORTANT: 
# - For localhost: http://localhost:3000
# - For Vercel: https://your-app.vercel.app (your actual domain)
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (for JWT encryption)
# PENTING: Generate random string dengan crypto dan JANGAN commit secret asli!
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production

# JWT Secret (for compatibility - use same value as NEXTAUTH_SECRET)
JWT_SECRET=your-nextauth-secret-change-in-production

# RajaOngkir API Key (untuk menghitung ongkir)
# Registrasi GRATIS di: https://rajaongkir.com/register
# Plan Gratis: 1000 requests/bulan, support JNE, POS, TIKI
# Setelah registrasi, copy API Key dari dashboard
RAJAONGKIR_API_KEY=your-rajaongkir-api-key
